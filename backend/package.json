{
  "name": "contatos-backend",
  "version": "1.0.0",
  "description": "Backend da aplicação Agenda de Contatos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cypress run --spec 'cypress/e2e/backend.cy.js'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "nodemon": "^3.0.2"
  }
}
